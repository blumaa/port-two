import React, { useCallback } from "react";

const Footer = () => {
  
  return (
    <div className="footer-container">
      <div className="item1"></div>
      <div className="item2"></div>
      <div className="item3">
        {/* <Button
          color="default"
          style={language.language === 'english' ? {color: '#ec6543ff'} :{ color: "#384a6cff" }}
          onClick={() => handleLngChange("en")}
        >
          EN
        </Button>
        <Button
          color="default"
          style={language.language === 'spanish' ? {color: '#ec6543ff'} :{ color: "#384a6cff" }}
          onClick={() => handleLngChange("es")}
        >
          ES
        </Button> */}
      </div>
    </div>
  );
};

export default Footer;
